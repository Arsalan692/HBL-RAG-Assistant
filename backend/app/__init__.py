"""HBL RAG Assistant — backend.

An offline retrieval assistant over HBL's policy and SOP corpus. Nothing in
this package may call a hosted API; every model runs on the local machine.
"""

__version__ = "0.1.0"
