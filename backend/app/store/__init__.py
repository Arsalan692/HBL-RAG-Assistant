"""Storage: the document registry, the keyword index and the vector store.

Nothing is imported here. `vectors` pulls in qdrant-client, which is installed
on the workstation and need not be on a laptop that only wants to read config.
"""
